## 👥 Top {{ limit or 10 }} Most Active Users

{% if top_users and top_users | length > 0 -%}
{% for user in top_users -%}
{% set display_name = user.display_name or user.author_name -%}
**{{ loop.index }}. {{ display_name }}** - {{ user.message_count | format_number }} messages
{% endfor -%}
{% else -%}
❌ No user data found
{% endif %} 