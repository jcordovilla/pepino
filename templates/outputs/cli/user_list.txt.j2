👥 Available Users for Analysis

{%- if items %}

Users (showing {{ showing_count }} of {{ total_count }}):
{%- for user in items %}
{{ loop.index }}. {{ user.display_name if user.display_name else user.author_name if user.author_name else user.username if user.username else user }}
   {%- if user.message_count %}
   Messages: {{ user.message_count | format_number }}
   {%- endif %}
   {%- if user.channels_active %}
   Active Channels: {{ user.channels_active | format_number }}
   {%- endif %}
   {%- if user.last_message %}
   Last Seen: {{ user.last_message | format_timestamp }}
   {%- endif %}

{%- endfor %}

{%- if has_more %}
... and {{ total_count - showing_count }} more users
Use --limit 0 to show all users
{%- endif %}
{%- else %}

No users found in the database.
{%- endif %}

Summary:
• Total users available: {{ total_count | format_number }}
• Ready for analysis with commands like:
  pepino analyze users --user username 